<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
       xmlns:beans="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/security
       http://www.springframework.org/schema/security/spring-security.xsd">

    <beans:bean id="customSuccessHandler" class="com.gabi.bankapp.authenticate.CustomSuccessHandler" />

    <http auto-config="true" use-expressions="true" >
        <intercept-url pattern="/list*" access="hasRole('ROLE_ADMIN')" />
        <!--<http-basic/>-->
        <form-login
                login-page="/user/login"
                authentication-failure-url="/user/login?error=true"
                username-parameter="username"
                password-parameter="password"
                authentication-success-handler-ref="customSuccessHandler"/>
        <!--just for testing purposes! never use it in production, it prevents XSS attacks-->
        <csrf disabled="true"/>
    </http>
    
    <!--<user-service>-->
        <!--<user name="gabi" password="{noop}gabi" authorities="ROLE_ADMIN" />-->
        <!--<user name="user" password="{noop}user" authorities="ROLE_USER"/>-->
    <!--</user-service>-->
    <user-service properties="WEB-INF/user.properties"/>

</beans:beans>